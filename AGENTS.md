# AGENTS.md

Mill's agent instructions live in [`CLAUDE.md`](CLAUDE.md) — read that
file first. This pointer exists so agent tools that look for
`AGENTS.md` (the cross-tool convention at
[agents.md](https://agents.md)) find the same context Claude Code
already uses, without a second copy that could drift out of sync.
