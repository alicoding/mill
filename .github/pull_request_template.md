## What

<!-- The change in one or two sentences, naming the goal/ADR it serves
     (e.g. "goal 0041", "ADR-0034") or the BACKLOG.md placement for
     ad-hoc work. -->

## Verification

<!-- How this was proven: local gate suite, unit/e2e cases added, the
     seeded example exercising it, or the manual-only registry entry
     if it's OS-bound (see .claude/rules/testing.md's layering). -->
