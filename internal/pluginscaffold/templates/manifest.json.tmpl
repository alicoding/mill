{
  "id": "{{.ID}}",
  "name": "{{.Label}}",
  "version": "0.1.0",
  "description": "What this plugin does, in one sentence.",
  "author": "",
  "minMillVersion": "{{.MinMillVersion}}",
  "icon": "icon.png",
  "capabilities": [],
  "contributes": {
    "commands": [
      { "id": "{{.ID}}.hello", "label": "{{.Label}}: say hello" }
    ]
  }
}
