// Mill's bundled Live view plugin (goal 0374): the board's own window
// onto an external tool's items, running on the same runtime plugin
// surface any out-of-tree plugin uses. Everything the view needs lives
// in its sandboxed entry page (view.html/view.js); there is nothing to
// activate in Mill's own context.
export function activate() {}
