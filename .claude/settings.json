{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"Reminder: if this is new/ad-hoc work, name where it lands vs docs/goals/BACKLOG.md before implementing. A goal enters Plan only with Precedent / Today / Gap filled in its goal file (CLAUDE.md Research step).\"}}'"
          }
        ]
      }
    ],
    "TaskCompleted": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/scripts/hook-task-completed.sh",
            "args": []
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/scripts/hook-build-guard.sh",
            "args": []
          },
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/scripts/hook-command-guard.sh",
            "args": []
          }
        ]
      },
      {
        "matcher": "Monitor",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Blocked: Monitor never wakes a subagent (its notification is lost). Poll in the foreground: a bounded `until grep -q ... ; do sleep 30; done` loop in a Bash call with timeout up to 600000, repeated in the next call if needed.' >&2; exit 2"
          }
        ]
      }
    ],
    "WorktreeRemove": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v golangci-lint >/dev/null 2>&1 && golangci-lint cache clean >/dev/null 2>&1; command -v \"$HOME/go/bin/golangci-lint\" >/dev/null 2>&1 && \"$HOME/go/bin/golangci-lint\" cache clean >/dev/null 2>&1; true"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/scripts/sweep-stale-servers.sh",
            "args": []
          }
        ]
      }
    ]
  }
}
